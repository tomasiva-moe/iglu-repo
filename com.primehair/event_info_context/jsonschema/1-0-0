{
  "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
  "description": "Schema for providing the GTM event name or trigger name as context.",
  "self": {
    "vendor": "com.primehair",
    "name": "event_info_context",
    "format": "jsonschema",
    "version": "1-0-0"
  },
  "type": "object",
  "properties": {
    "eventName": {
      "description": "The name of the GTM event (e.g., from dataLayer event key or a custom event name).",
      "type": "string",
      "maxLength": 255
    }
  },
  "required": ["eventName"],
  "additionalProperties": false
}
